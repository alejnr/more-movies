<%- include('partials/header') -%>
  <meta
    name="description"
    content="<%= actionMovieOverview.substring(0, 150) + '...' %>"
  />
  <title><%= requestedactionMovieName %><%= requestedactionMovieTitle %></title>

  <body>

    <div class="image-wrap">
      <div class="img-content">
        <img src="<%= image + requestedactionMovieBackdropImage %>" alt="" />
      </div>
      <div class="overlay"></div>
    </div>
    <div class="banner-content">
      <div>
        <div class="row no-gutters">
          <div class="col-md-4">
            <img
              src="<%= image + requestedactionMoviePosterImage %>"
              class="card-img poster"
              alt="<%= requestedactionMovieName %><%= requestedactionMovieTitle %>"
            />
          </div>
          <div class="col-md-8">
            <div class="card-body">
              <h1 class="card-title">
                <a target="_blank" href="https://www.google.com/search?q=<%= requestedactionMovieName %><%= requestedactionMovieTitle %>">
                  <%= requestedactionMovieName %><%= requestedactionMovieTitle %>
                </a>
              </h1>
              <p class="card-text text-left"><%= actionMovieOverview %></p>
            </div>
          </div>
        </div>
      </div>
    </div>

<%- include('partials/footer') -%>
